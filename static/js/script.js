function seeSeatChart(element, track) {
    var seat_button = element.target.id;
}

const trackLayout = document.getElementById("track_image").innerHTML;
document.addEventListener("click", seeSeatChart)