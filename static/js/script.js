function clickHandler(element) {
    stand_name = element.target.id;
    var letters = ["A", "B", "C", "D"];
    for (let i = 0; i < letters.length; i++) {
        if (stand_name === letters[i]) {

        }
    }
}